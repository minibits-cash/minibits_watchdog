# Dashboard port. Change and restart — no rebuild.
PORT=3006

# Backend the dashboard proxies /api/* to. Read at server start, so this is a
# restart-only change. Deliberately NOT NEXT_PUBLIC_*, which would inline it into
# the client bundle at build time and require a rebuild to change.
BACKEND_URL=http://127.0.0.1:3005
